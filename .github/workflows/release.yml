name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build-release:
    name: Build Release Artifacts
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: plenumnet-kernel-linux-x86_64
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            artifact: plenumnet-kernel-linux-aarch64

    steps:
      - uses: actions/checkout@v4

      - name: Check source exists
        id: check
        run: |
          if [ -f "src/kernel/Cargo.toml" ]; then
            echo "has_kernel=true" >> $GITHUB_OUTPUT
          else
            echo "has_kernel=false" >> $GITHUB_OUTPUT
          fi
          if [ -f "src/tsl/Cargo.toml" ]; then
            echo "has_tsl=true" >> $GITHUB_OUTPUT
          else
            echo "has_tsl=false" >> $GITHUB_OUTPUT
          fi
          if [ -f "src/thdl/Cargo.toml" ]; then
            echo "has_thdl=true" >> $GITHUB_OUTPUT
          else
            echo "has_thdl=false" >> $GITHUB_OUTPUT
          fi

      - name: Install Rust toolchain
        if: steps.check.outputs.has_kernel == 'true'
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross-compilation tools
        if: steps.check.outputs.has_kernel == 'true' && matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Build kernel (release)
        if: steps.check.outputs.has_kernel == 'true'
        working-directory: src/kernel
        run: cargo build --release --target ${{ matrix.target }} --all-features
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc

      - name: Build TSL compiler (release)
        if: steps.check.outputs.has_tsl == 'true'
        working-directory: src/tsl
        run: cargo build --release

      - name: Build THDL synthesizer (release)
        if: steps.check.outputs.has_thdl == 'true'
        working-directory: src/thdl
        run: cargo build --release

      - name: Package artifacts
        if: steps.check.outputs.has_kernel == 'true'
        run: |
          mkdir -p release/${{ matrix.artifact }}
          cp -r src/kernel/target/${{ matrix.target }}/release/libplenumnet_kernel* release/${{ matrix.artifact }}/ 2>/dev/null || true
          cp -r src/tsl/target/release/tsl* release/${{ matrix.artifact }}/ 2>/dev/null || true
          cp -r src/thdl/target/release/thdl* release/${{ matrix.artifact }}/ 2>/dev/null || true
          tar -czf ${{ matrix.artifact }}.tar.gz -C release ${{ matrix.artifact }}

      - name: Upload artifact
        if: steps.check.outputs.has_kernel == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}.tar.gz

  create-release:
    name: Create GitHub Release
    needs: build-release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        continue-on-error: true

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            **/*.tar.gz
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref, 'alpha') || contains(github.ref, 'beta') }}
