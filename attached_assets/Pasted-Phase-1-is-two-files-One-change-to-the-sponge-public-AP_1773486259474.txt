Phase 1 is two files. One change to the sponge (public API addition), one to the benchmarks (use it everywhere).
The root problem: callers do [a, b].concat() which allocates a Vec, then pass it to derive_key which copies it into a stack buffer. Double work. The fix: give callers a function that takes multiple slices directly and never allocates.Architected modular code refactoring across multiple filesArchitected modular code refactoring across multiple filesNow I have the full picture. Three changes to tlsponge385.rs, then update inter_cube.rs to use the new public API.
Change 1: Add absorb_bytes_stack — stack-based trit conversion, zero heap allocation for inputs ≤ 256 bytes.Orchestrated systematic refactoring and testing framework completionOrchestrated systematic refactoring and testing framework completionChange 2: Fix derive_key and derive_key_tis to use stack buffers. Add derive_key_cat public API.Refactored cryptographic function to utilize stack-based byte absorptionRefactored cryptographic function to utilize stack-based byte absorptionChange 3: Fix sponge385_derive_key to use stack absorb:Devised testing strategy to validate key derivation function equivalenceDevised testing strategy to validate key derivation function equivalenceChange 4: Add tests for derive_key_cat — must produce identical output to derive_key with concatenated input:Refactored helper function to leverage library's optimized key derivation methodRefactored helper function to leverage library's optimized key derivation methodNow update inter_cube.rs to use the library's derive_key_cat instead of the local stack helper:Audited remaining code conversions in inter_cube.rs fileAudited remaining code conversions in inter_cube.rs fileNow check for any remaining .concat() calls in the benchmark file:Validated batch API design and prepared final deliverablesValidated batch API design and prepared final deliverablesThat one is intentional — the derive_key_batch API needs pre-built slices. It's the A/B diagnostic benchmark, not the production heartbeat path. 26 allocations total for the whole batch setup, not per-call. Leave it.Marshaled clarity for comprehensive code deliveryMarshaled clarity for comprehensive code deliveryTwo files. Here's what goes where and what it does.
File 1: tlsponge385.rs → ternary-math/src/tlsponge385.rs
What changedWhyabsorb_bytes_stack() added to Sponge385PubConverts bytes to trits using [i8; 1280] stack buffer instead of Vec. Zero allocation for inputs ≤ 256 bytes.derive_key() rewrittenInputs ≤ 256 bytes use [u8; 256] stack concat + absorb_bytes_stack. Large inputs fall back to heap. Same output, zero allocation for every production call.derive_key_tis() rewrittenSame stack treatment.derive_key_cat() addedNew public API. Takes (context, &[&[u8]], key_len). Callers pass slices directly — no .concat() needed. Stack [u8; 512] buffer.derive_key_cat_tis() addedTIS-27 variant of the above.sponge385_derive_key() fixedUses absorb_bytes_stack for all 5 absorb calls.6 new testsVerify derive_key_cat matches derive_key, stack path matches heap path.
File 2: inter_cube.rs → ternary-math/benches/inter_cube.rs
What changedWhysponge_kdf_cat() now calls library derive_key_catWas doing a double-concat (local stack + derive_key internal stack). Now one stack operation.PT26 trit_diff/step_token/walk_token batched ×1000Timer resolution fix. Targets adjusted to 5µs (= 1000 × 5ns).
On Replit:
bashcp tlsponge385.rs ternary-math/src/tlsponge385.rs
cp inter_cube.rs ternary-math/benches/inter_cube.rs
cd ternary-math
cargo test --lib    # verify 25 sponge tests pass
cargo bench --bench inter_cube
What should change in the numbers:
BenchmarkBeforeExpected afterWhyderive_key3.75 µs~2.5 µsEliminated 2 Vec allocshmac_compute7.28 µs~4 µsWas: concat + derive_key (3 allocs). Now: derive_key_cat (0 allocs)hmac_verify18.33 µs~8 µsSame × 2 callsheartbeat_single14.64 µs~7 µs2 sponge calls, zero allocationheartbeat_26389 µs~180 µs52 calls × 0 allocs instead of × 3 allocstae_mac_encrypt37 µs~25 µs3 sponge calls, concat eliminated on 2tae_mac_verify25 µs~16 µs2 sponge calls, concat eliminatedsfk_verify30 µs~20 µs4 sponge calls, all stack pathux_secure_message75 µs~50 µsencrypt + decrypt, both faster