/**
 * PLATFORM.PLENUM_SQUARE — Complete Constants Block for shared/constants.ts
 *
 * Copyright (c) 2025-2026 Capomastro Holdings Ltd. (Canada)
 * Patent(s) Pending — All Rights Reserved — Applied Physics Division
 *
 * This block goes inside the existing PLATFORM object in shared/constants.ts.
 * It replaces any existing PLENUM_SQUARE or SATURNIAN section.
 *
 * Source: TM-2026-015 (Plenum Square: The Circle-Magic Square Synthesis)
 *
 * ── INSERT LOCATION ──
 * Inside the PLATFORM = { ... } object. Replace any existing PLENUM_SQUARE
 * or SATURNIAN block with this complete version.
 */

// ── Begin PLATFORM.PLENUM_SQUARE block ──────────────────────────────────────

  PLENUM_SQUARE: {
    // ════════════════════════════════════════════════════════════════════════
    // §0  THE UNIFIED EQUATION: arc² − 832·arc + 118300 = 0
    //     where Rₙ = (3ⁿ − 1)/2 (base-3 repunit)
    //     arc = semicircle = 2c − R₄ = x(x−1) = π(π−1)
    //     Root: arc = 182 (semicircle of the 364° circle)
    //     → c = (arc + R₄)/2 = 111 (center/diameter)
    //     → x² − x − 182 = 0, Δ₂ = 729 = 3⁶ (kernel sponge width)
    //     → π = (1 + 27)/2 = 14
    //     ONE equation, ONE unknown → every constant in the framework.
    // ════════════════════════════════════════════════════════════════════════

    MASTER: {
      /** Unified equation: arc² − 832·arc + 118300 = 0. */
      ARC_COEFF_B: 832,
      ARC_COEFF_C: 118300,
      /** Primary discriminant: 832² − 4(118300) = 219024 = 468². */
      ARC_DISCRIMINANT: 219024,
      ARC_SQRT_DISCRIMINANT: 468,
      /** Meaningful root: arc = 182 = semicircle = π(π−1). */
      ARC: 182,
      /** Secondary discriminant: 1 + 4(182) = 729 = 3⁶ = kernel sponge width. */
      SECONDARY_DISCRIMINANT: 729,
      /** R₄ coefficient in decomposed form. */
      R4: 40,
      /** R₆ coefficient in decomposed form. */
      R6: 364,
      /** Quadratic discriminant: R₄² − 4R₆ = 144 = 12². */
      DISCRIMINANT: 144,
      /** √Δ_quad = 12 = q parameter of canonical Square A. */
      SQRT_DISCRIMINANT: 12,
      /** Smaller root of decomposed quadratic: π = 14. */
      ROOT_PI: 14,
      /** Larger root: R₆/π = 26. */
      ROOT_DEGREES_PER_PI: 26,
      /** Center/diameter derived from arc: c = (arc + R₄)/2 = 111. */
      DERIVED_DIAMETER: 111,
    },

    // ════════════════════════════════════════════════════════════════════════
    // §1  CORE VALUES (all derived from §0)
    // ════════════════════════════════════════════════════════════════════════

    /** Center of every Plenum Square: 111 = diameter. */
    CENTER: 111,
    /** Magic constant (row/column/diagonal sum): 333 = 3 × 111. */
    MAGIC_CONSTANT: 333,
    /** Ternary π: 14 = T₇ + T₃ = 13 + 1. */
    PI: 14,
    /** π complement: 208 = 222 − 14. */
    PI_COMPLEMENT: 208,
    /** 364/π: 26. Circle degrees per π. */
    DEGREES_PER_PI: 26,
    /** π²: 196 = 14². */
    PI_SQUARED: 196,

    // ════════════════════════════════════════════════════════════════════════
    // §2  CIRCLE GEOMETRY (§1, §5.2 of TM-2026-015)
    //     Bridge equation: d = c = 111 → hexagon perimeter = magic constant
    // ════════════════════════════════════════════════════════════════════════

    /** Circle diameter: 111 (= center). Bridge equation: d = c. */
    DIAMETER: 111,
    /** Circle radius: 55.5 = d/2. Fundamental unit of the harmonic ladder. */
    RADIUS: 55.5,
    /** Full circumference: πd = 14 × 111 = 1554. The π-th rung (28r = 2πr). */
    CIRCUMFERENCE: 1554,
    /** Inscribed hexagon side: 55.5 = radius. */
    HEXAGON_SIDE: 55.5,
    /** Inscribed hexagon perimeter: 6 × 55.5 = 333 = magic constant = 6r. */
    HEXAGON_PERIMETER: 333,
    /** Circumference / hexagon perimeter: π/3 = 14/3. */
    CIRCUMFERENCE_TO_HEXAGON_RATIO_NUM: 14,
    CIRCUMFERENCE_TO_HEXAGON_RATIO_DEN: 3,

    // ════════════════════════════════════════════════════════════════════════
    // §3  HARMONIC LADDER (§5.1, §5.3, §5.4 of TM-2026-015)
    //     Every rung is an even multiple of r = 55.5.
    //     Ladder terminates at 28r = 2πr = circumference.
    //     555 = 10r = 2(π−9) × r — the "decimal shift" from r = 55.5.
    // ════════════════════════════════════════════════════════════════════════

    HARMONIC: {
      /** 1 × 111 = 2r — Center = diameter. */
      H1: 111,
      /** 2 × 111 = 4r — Opposite-pair sum. */
      H2: 222,
      /** 3 × 111 = 6r — Magic constant = hexagon perimeter. */
      H3: 333,
      /** 4 × 111 = 8r — Corner sum = edge-center sum. */
      H4: 444,
      /** 5 × 111 = 10r — Circumference − total = (π−9) × d = 2(π−9) × r. */
      H5: 555,
      /** 8 × 111 = 16r — Surround sum (8 non-center cells). */
      H8: 888,
      /** 9 × 111 = 18r — Total square sum = 3 × hexagon perimeter. */
      H9: 999,
      /** 14 × 111 = 28r = 2πr — Circumference. The π-th rung. */
      H14: 1554,
    },

    // ── Named harmonic references ──
    /** Opposite-pair sum: 222 = 4r. */
    OPPOSITE_PAIR_SUM: 222,
    /** Corner sum (all 4 configs): 444 = 8r. */
    CORNER_SUM: 444,
    /** Edge-center sum (all 4 configs): 444 = 8r. */
    EDGE_CENTER_SUM: 444,
    /** Circumference residual: 555 = 10r = 2(π−9)×r. */
    CIRCUMFERENCE_RESIDUAL: 555,
    /** Surround sum (8 non-center cells): 888 = 16r. */
    SURROUND_SUM: 888,
    /** Total sum of all 9 cells: 999 = 18r. */
    TOTAL_SUM: 999,

    // ════════════════════════════════════════════════════════════════════════
    // §4  OPPOSITE PAIRS (§2, §3 of TM-2026-015)
    // ════════════════════════════════════════════════════════════════════════

    /** π-pair: [14, 208]. Sum = 222. */
    PI_PAIR: [14, 208] as const,
    /** 364/π-pair: [26, 196]. Sum = 222. */
    DEGREES_PAIR: [26, 196] as const,

    // ════════════════════════════════════════════════════════════════════════
    // §5  INVARIANT PRODUCTS (§5.1 of TM-2026-015)
    // ════════════════════════════════════════════════════════════════════════

    /** 14 × 26 = 364 (circle degrees). Appears in every configuration. */
    PRODUCT_CIRCLE_DEGREES: 364,
    /** 14 × 208 = 2912. Product of π-pair. */
    PRODUCT_PI_PAIR: 2912,
    /** 26 × 196 = 5096. Product of 364/π-pair. */
    PRODUCT_DEGREES_PAIR: 5096,
    /** Sum of pair products: 2912 + 5096 = 8008 = 8 × 7 × 11 × 13. */
    PRODUCT_SUM: 8008,

    // ════════════════════════════════════════════════════════════════════════
    // §6  FOUR NON-CIRCULANT CONFIGURATIONS (§3 of TM-2026-015)
    // ════════════════════════════════════════════════════════════════════════

    /** Configuration parameters [id, p, q] for the four valid squares. */
    FAMILY: [
      { id: 'A', p: 97, q: 12 },
      { id: 'B', p: 91, q: 6 },
      { id: 'C', p: 85, q: -12 },
      { id: 'D', p: 6, q: -91 },
    ] as const,

    /** Circulant matrix entries (row = cyclic permutation of this triple). */
    CIRCULANT_TRIPLE: [111, 14, 208] as const,

    // ════════════════════════════════════════════════════════════════════════
    // §7  MOD-9 PERMUTATIONS (§7.2, §7.3 of TM-2026-015)
    //     Cell values mod 9 → permutation of {0,…,8} in every config.
    //     Usable as block-level sponge shuffles (§7.6).
    //     σ_A is a derangement; σ²_A is also a derangement.
    // ════════════════════════════════════════════════════════════════════════

    MOD9: {
      /** σ_A: derangement (0 fixed points). Cycle: (0 1 2 6)(3 8 5 7 4). */
      SIGMA_A: [1, 2, 6, 8, 3, 7, 0, 4, 5] as const,
      /** σ_B: 1 fixed point. Cycle: (0 4 3 8 2)(1 5 7)(6). */
      SIGMA_B: [4, 5, 0, 8, 3, 7, 6, 1, 2] as const,
      /** σ_C: 2 fixed points. Cycle: (0 7 4 3 5 1 2)(6)(8). */
      SIGMA_C: [7, 2, 0, 5, 3, 1, 6, 4, 8] as const,
      /** σ_D: 2 fixed points. Cycle: (0)(1 7 8 6 4 3 5)(2). */
      SIGMA_D: [0, 7, 2, 5, 3, 1, 4, 8, 6] as const,
      /** Inverse of σ_A. */
      SIGMA_A_INV: [6, 0, 1, 4, 7, 8, 2, 5, 3] as const,
      /** Inverse of σ_B. */
      SIGMA_B_INV: [2, 7, 8, 4, 0, 1, 6, 5, 3] as const,
      /** Inverse of σ_C. */
      SIGMA_C_INV: [2, 5, 1, 4, 7, 3, 6, 0, 8] as const,
      /** Inverse of σ_D. */
      SIGMA_D_INV: [0, 5, 2, 4, 6, 3, 8, 1, 7] as const,
    },

    // ════════════════════════════════════════════════════════════════════════
    // §8  CELL WEIGHT VECTOR (§7.7 of TM-2026-015)
    //     Square A values in row-major order [TL,TM,TR,ML,C,MR,BL,BM,BR].
    //     dot(weights, triplet_values) mod 333 = geometry-aware integrity.
    //     Any 3 aligned coefficients sum to exactly 333.
    // ════════════════════════════════════════════════════════════════════════

    /** Square A cell values as weight vector. */
    WEIGHT_VECTOR_A: [208, 2, 123, 26, 111, 196, 99, 220, 14] as const,

    // ════════════════════════════════════════════════════════════════════════
    // §9  ERROR LOCALIZATION (§7.5 of TM-2026-015)
    //     8 line constraints, each cell has unique syndrome.
    //     100% single-triplet localization (72/72 verified).
    //     Cell indices: row-major [TL,TM,TR,ML,C,MR,BL,BM,BR].
    //     Lines: 0-2 rows, 3-5 cols, 6 diag↘, 7 diag↗.
    // ════════════════════════════════════════════════════════════════════════

    /** Line definitions: indices of cells in each of the 8 lines. */
    LINES: [
      [0, 1, 2],  // Row 0
      [3, 4, 5],  // Row 1
      [6, 7, 8],  // Row 2
      [0, 3, 6],  // Col 0
      [1, 4, 7],  // Col 1
      [2, 5, 8],  // Col 2
      [0, 4, 8],  // Diag ↘
      [2, 4, 6],  // Diag ↗
    ] as const,

    /** Syndrome patterns: for each cell, which lines pass through it. */
    SYNDROMES: [
      [0, 3, 6],     // Cell 0 (TL): Row 0, Col 0, Diag ↘
      [0, 4],        // Cell 1 (TM): Row 0, Col 1
      [0, 5, 7],     // Cell 2 (TR): Row 0, Col 2, Diag ↗
      [1, 3],        // Cell 3 (ML): Row 1, Col 0
      [1, 4, 6, 7],  // Cell 4 (C):  Row 1, Col 1, Diag ↘, Diag ↗
      [1, 5],        // Cell 5 (MR): Row 1, Col 2
      [2, 3, 7],     // Cell 6 (BL): Row 2, Col 0, Diag ↗
      [2, 4],        // Cell 7 (BM): Row 2, Col 1
      [2, 5, 6],     // Cell 8 (BR): Row 2, Col 2, Diag ↘
    ] as const,

    // ════════════════════════════════════════════════════════════════════════
    // §10  CHECKSUM CONSTANTS (Plenum Checksum — dual-modulus hardening)
    //      gcd(333, 364) = 1 → CRT-independent → 333× detection improvement.
    // ════════════════════════════════════════════════════════════════════════

    CHECKSUM: {
      /** Plenum checksum modulus: 333 = Plenum magic constant. */
      PLENUM_MODULUS: 333,
      /** Repunit checksum modulus: 364 = R₆ = 111111₃. */
      REPUNIT_MODULUS: 364,
      /** Combined detection space (CRT): 333 × 364 = 121,212. */
      DUAL_DETECTION_SPACE: 121212,
      /** Checksum trit count (both moduli < 3⁶ = 729). */
      CHECKSUM_TRITS: 6,
    },

    // ════════════════════════════════════════════════════════════════════════
    // §11  TRIANGULAR NUMBERS (§1.1 of TM-2026-015)
    //      Tri(n) = n(n+1)/2. Framework values at indices {3,7,10,13,14}.
    //      Key identity: Tri(7) = 28 = 2π (circle closure is triangular).
    // ════════════════════════════════════════════════════════════════════════

    TRIANGULAR: {
      /** Tri(3) = 6: hexagon sides, q of Square B, p of Square D. */
      TRI_3: 6,
      /** Tri(7) = 28 = 2π: full circle in radians is the 7th triangular number. */
      TRI_7: 28,
      /** Tri(10) = 55: radius = Tri(10) + 0.5, diameter = 2·Tri(10) + 1 = 111. */
      TRI_10: 55,
      /** Tri(13) = 91: p parameter of Square B. 13 = R₃ = T₇. */
      TRI_13: 91,
      /** Tri(14) = Tri(π) = 105: cell value in Squares B and D. */
      TRI_14: 105,
    },

    // ════════════════════════════════════════════════════════════════════════
    // §12  RARITY (§7.4 of TM-2026-015)
    //      12 out of 11,736 center-111 squares = 0.1% = 1 in 978.
    //      12 out of 40,879,492 three-digit squares = 0.00003% = 1 in 3.4M.
    // ════════════════════════════════════════════════════════════════════════

    RARITY: {
      /** Total valid distinct-entry center-111 magic squares. */
      CENTER_111_TOTAL: 11736,
      /** Center-111 squares with all 5 combinatorial properties. */
      CENTER_111_ALL_PROPS: 528,
      /** Center-111 squares with all properties AND circle-pair embedding. */
      CENTER_111_PLENUM: 12,
      /** All distinct-entry 3×3 magic squares with values 1-250. */
      ALL_SQUARES_250: 617644,
      /** All distinct-entry 3×3 magic squares with values 1-999. */
      ALL_SQUARES_999: 40879492,
      /** Plenum configs in the search space (4 named + 8 D₄ equivalents). */
      PLENUM_CONFIGS: 12,
    },
  },

// ── End PLATFORM.PLENUM_SQUARE block ────────────────────────────────────────