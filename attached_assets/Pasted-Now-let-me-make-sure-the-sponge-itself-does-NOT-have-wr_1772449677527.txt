Now let me make sure the sponge itself does NOT have wrapping arithmetic — trit_add should stay strict and crash on invalid inputs so you catch bugs instead of hiding them:Diagnosed overflow bug and validated optimization improvementsDiagnosed overflow bug and validated optimization improvementsVerify sponge has no wrapping arithmeticScriptGood - only in a comment, not in actual code. The sponge is clean.
Now let me verify there are no other cases. Let me also revert any wrapping changes the user might have made.DoneGood — no wrapping in the sponge. Now verify the complete picture:Identified bug sources and prioritized critical file fixes for deploymentIdentified bug sources and prioritized critical file fixes for deploymentFinal audit of all absorb callsScriptLet me verify the remaining calls:

firmware_sign.rs:108 — &[0i8] — 0 is a valid trit ✓
All .trits absorbs — these come from TernaryDigest which validates on construction ✓
hmac.rs:54,59 — ikey/okey — let me check what these are
hmac.rs:37 was in the previous scan too
kdf.rs:68 — result.trits from TernaryDigest ✓
perf_bench.rs — benchmark input, should be valid trits ✓
signature.rs:120 — idx_trits derives valid trits from the index using modulo arithmetic ✓

Now I need to trace back through hmac.rs to understand where ikey and okey originate.Show moreCheck hmac key handlingScriptLet me check what trit_xor produces:Check trit_xor and pad constantsScriptFound another bug! trit_xor in hmac.rs uses the same broken % 3 pattern. But it takes trit inputs (padded_key trits XOR'd with IPAD_TRIT=1 or OPAD_TRIT=-1), so both inputs are valid trits. The % 3 will work correctly for valid trit inputs since (a+1+b+1) with a,b ∈ {-1,0,1} gives range [0,4], and % 3 on [0,4] gives [0,2], minus 1 gives [-1,1]. So this works.
I could optimize it away for consistency and performance, but it's not a correctness issue since the inputs are guaranteed to be valid trits. Let me refocus on what matters—I need to compile the list of files ready to push: sponge.rs with the optimized wrapping fix, ternary_lattice.rs, phase_cnsa.rs, signature.rs, and x509.rs all with their absorb_bytes corrections.
All the remaining absorb() calls are working with valid trit inputs, and while hmac.rs could use a performance pass on its modulo operations, that's a separate concern from correctness. Now I'll revert any wrapping changes I made to trit_add and prepare the final set of files.Show moreDoneAll remaining absorb() calls pass valid trits — TernaryDigest.trits, literal 0i8, or computed trit arrays. Clean.
One more thing in hmac.rs — that trit_xor uses the same % 3 pattern, but it's not a crash bug since both inputs are valid trits. Worth fixing for performance but not blocking.
Let me package everything:Identified overflow bug and orchestrated comprehensive performance validationIdentified overflow bug and orchestrated comprehensive performance validationConfirm sponge trit_add is strict (no wrapping)ScriptGood - strict, no wrapping. Now let me copy all 5 changed files to outputs.DoneGood — strict arithmetic, will panic on invalid inputs. That's correct. Now copy all changed files:Diagnosed overflow bug and validated optimization gainsDiagnosed overflow bug and validated optimization gainsPackage all 5 changed filesScriptLet me show the actual diffs for each of the non-sponge files to make it clear what changed.Show clear diff summaryScriptLet me present all the files to the user.Presented 5 filesAll 5 files are ready.DoneFive files, all drop into src/kernel/src/crypto/. Here's what to push instead of the wrapping fix:
sponge.rs — Optimized permutation. Same algorithm, first-principles math, no lookup tables. Strict trit_add that will crash on non-trit inputs (that's the point — it catches bugs).
Four caller fixes — The actual bug. These files were feeding byte values (0–255 range) into absorb() which expects trits ({-1, 0, +1}). Fixed to absorb_bytes() which properly converts bytes to balanced ternary before absorption.
Important: Do not push the wrapping arithmetic version of trit_add. That makes the sponge silently accept any i8 value without complaint, which means the next time someone writes absorb(&[some_byte as i8]) it won't crash — it'll just produce wrong hashes quietly. The strict version is your contract enforcement: if it panics, the caller is broken, not the sponge.
Breaking change: These fixes will change every hash output from the sponge in code paths that touch ternary_lattice, phase_cnsa, signature, and x509. The old outputs were computed by hashing raw byte integers as if they were trits — they were never correct. Any stored KAT vectors from those paths need regeneration.