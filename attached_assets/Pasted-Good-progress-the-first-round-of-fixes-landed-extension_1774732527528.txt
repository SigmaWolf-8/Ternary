Good progress — the first round of fixes landed (extensions pinned at v6.0.2, `Platform`/`Comments` gone, `-arch` passing). Now you've got a fresh crop of WiX v6 schema errors. Let me trace each one.Good — I've mapped every error. There are **five distinct WiX v6 schema violations**, all fixable. Let me produce the corrected files.

Here's what each error is and the fix:

**Error 1 — WIX0006 (×3): `Property/@Value` cannot be empty string**
Lines 39/41/44 in the rendered WXS. The Launcher is a `tray_agent`, not a service, and has no passphrase actions. So `{{PASSPHRASE_REQUIRED}}` → `""`, `{{SERVICE_CONFIG_REQUIRED}}` → `""`, and `SERVICE_CUSTOM_ACCOUNT` is hardcoded `Value=""`. WiX v6 rejects `Value=""` — you must either omit the property entirely or omit the `Value` attribute and mark it `Secure="yes"`.

**Error 2 — WIX0070: `MsiHiddenProperties` cannot be authored**
WiX v6 manages the `MsiHiddenProperties` special property internally. The individual properties already have `Hidden="yes"` on them (PASSPHRASE_FILE, PASSPHRASE_TEMPFILE, SERVICE_ACCOUNT_PASSWORD), so the explicit `MsiHiddenProperties` line is both redundant and illegal in v6.

**Error 3 — WIX0005 (×2): `Component` cannot have `Condition` child element**
WiX v4+ moved component conditions from a child `<Condition>` element to a `Condition="..."` *attribute* on the `<Component>` tag. This hits both `PathEntry` (in the template) and `AutostartEntry` (generated by `generate_tray_component()` in `wix.rs`).

**Error 4 — WIX0230: Auto-GUID component needs explicit KeyPath**
`PathEntry` has `Guid="*"` but only contains an `<Environment>` element — no KeyPath. WiX can't auto-generate a GUID without one. Fix: add a registry sentinel value as KeyPath.

Let me produce both corrected files:Now the corrected `wix.rs` — I need to change `generate_product_wxs()`, `generate_tray_component()`, and remove `compute_hidden_properties()`:Good — all five fixes are in place across both files. Let me do a final diff to make sure nothing else was disturbed:Clean diffs — exactly 7 surgical changes across both files, nothing else touched.**Drop-in destinations:**
- `product.wxs` → `C:\PlenumNET\tools\plenum-pack\templates\product.wxs`
- `wix.rs` → `C:\PlenumNET\tools\plenum-pack\src\wix.rs`

**Summary of all 7 changes across both files:**

| # | Error | File | Fix |
|---|-------|------|-----|
| 1 | WIX0006: `PASSPHRASE_REQUIRED Value=""` | Both | Conditional emission — only emit the `<Property>` when value is `"1"` |
| 2 | WIX0006: `SERVICE_CONFIG_REQUIRED Value=""` | Both | Same pattern — emit full element or nothing |
| 3 | WIX0006: `SERVICE_CUSTOM_ACCOUNT Value=""` | `product.wxs` | Remove `Value=""`, add `Secure="yes"` (WiX allows valueless properties when marked Secure) |
| 4 | WIX0070: `MsiHiddenProperties` authored | Both | Removed entirely — WiX v6 manages this internally; individual properties already carry `Hidden="yes"` |
| 5 | WIX0005: `<Condition>` child on `PathEntry` | `product.wxs` | Moved to `Condition="..."` attribute on `<Component>` |
| 6 | WIX0005: `<Condition>` child on `AutostartEntry` | `wix.rs` | Same — attribute instead of child element |
| 7 | WIX0230: `PathEntry Guid="*"` needs KeyPath | `product.wxs` | Added `<RegistryValue>` sentinel with `KeyPath="yes"` |

After replacing both files, rebuild plenum-pack (`cargo build --release -p plenum-pack`) and re-run the installer. The `Platform`/`Comments` fixes from round one plus these five new fixes should clear the entire WiX v6 error surface.