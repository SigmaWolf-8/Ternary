Phase 3 is Merkle path optimization. The remaining Phase 4 items are already done (timer batching complete, AES-GCM marked simulated). Let me check what's left.Analyzed performance bottlenecks and identified actionable optimization targetsAnalyzed performance bottlenecks and identified actionable optimization targetsMerkle already uses sponge_kdf_cat from the earlier pass. The remaining cost is 41 sponge calls × output allocation inside squeeze() — that's library-level, already fixed by Phase 1's absorb_bytes_stack. But there are other Forge items with easy allocation fixes that Phase 3 should pick up. Let me find all of them.Audited cryptographic allocations and strategized remaining implementation fixesAudited cryptographic allocations and strategized remaining implementation fixesIdentity and Lattice are already fixed (use sponge_kdf_cat). Tunnel and Phase still allocate Vecs. Let me fix everything remaining.Debugged encryption phase vector handling for recombination logicDebugged encryption phase vector handling for recombination logicNow Phase encryption — eliminate Vec allocations in split/recombine:Refactored cryptographic buffer implementations for phase completionRefactored cryptographic buffer implementations for phase completionNow fix T-AE-MAC — eliminate remaining Vec allocations:Identified and resolved KEM decapsulation implementation issueIdentified and resolved KEM decapsulation implementation issueFix KEM decaps — stack ciphertext:Debugged AES-GCM encryption implementation with XOR operationsDebugged AES-GCM encryption implementation with XOR operationsFix AES-GCM — stack XOR buffers:Deliberated performance targets and allocation optimization strategiesDeliberated performance targets and allocation optimization strategiesNow let me find every remaining vec! and Vec allocation in benchmark functions:Audited memory allocations and prioritized optimization targetsAudited memory allocations and prioritized optimization targetsFix the remaining easy ones — PT26 message building, TIS-27 inputs, TDNS, and CON:Pivoted to address TIS-27 input correctionsPivoted to address TIS-27 input correctionsFix TIS-27 — stack arrays instead of Vec collect:Reconciled technical classifications and addressing parametersReconciled technical classifications and addressing parametersFix TDNS — stack arrays:Identified code optimization issue requiring vector capacity adjustmentIdentified code optimization issue requiring vector capacity adjustmentFix CON derive_tunnel_key — sponge_kdf_cat:Identified and resolved vector allocation inefficiencyIdentified and resolved vector allocation inefficiencyFix epoch_rekey — stack buffer for HMAC key material:Optimized security by replacing dynamic allocation with stack bufferOptimized security by replacing dynamic allocation with stack bufferWhat was fixed:
AreaWhatAllocations eliminatedPT26 sign/verify/verify_parallelVec::with_capacity(80) → sponge_kdf_cat3 per callTunnel auth_responseVec::with_capacity(98) → sponge_kdf_cat1Tunnel handshake2 × Vec::with_capacity(128) → sponge_kdf_cat2T-AE-MAC encrypt/decryptVec XOR collect + vec! ciphertext → stack arrays3 per callPhase split/recombineVec share + Vec result → stack arrays5 totalPhase batch_recombinevec![0u8; 256] → stack array10 (loop)AES-GCM encrypt/decryptVec XOR collect + vec! → stack arrays4 totalKEM decapsvec![42u8; n] → stack array1 per callTIS-27 hash_54/absorbVec collect → stack arrays2TDNS scan/repunitVec collect → stack arrays2CON derive_tunnel_keyVec::with_capacity(58) → sponge_kdf_cat1Epoch rekeyVec in loop → stack array26
Remaining Vec usage (intentional — can't avoid):

Batch API setup (TL-DSA v2 keygen, A/B heartbeat) — the derive_key_batch API signature requires &[&[u8]]
cached_hmac_keys OnceLock — one-time init, not hot path
sponge_kdf return value — derive_key returns Vec<u8>, that's the library API

The file at /mnt/user-data/outputs/inter_cube.rs has all Phase 2, 3, and 4 changes. Drop it alongside the Phase 1 tlsponge385.rs and run the full suite.Continue5:27 AMVerified code locations and confirmed intentional designVerified code locations and confirmed intentional designThree remaining Vecs — all intentional: line 71 is cached_hmac_keys (one-time init), line 1180 is the registry vec![], line 1451 is the measurement timer loop. Zero in hot paths.Synthesized multiple documents for coherent summarySynthesized multiple documents for coherent summaryTwo files. All four phases complete.
bashcp tlsponge385.rs ternary-math/src/tlsponge385.rs
cp inter_cube.rs ternary-math/benches/inter_cube.rs
cd ternary-math
cargo test --lib
cargo bench --bench inter_cube
The markdown report auto-generates in the ternary-math directory. Push to GitHub when it passes.