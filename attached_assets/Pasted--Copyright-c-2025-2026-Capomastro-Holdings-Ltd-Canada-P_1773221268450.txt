// Copyright (c) 2025-2026 Capomastro Holdings Ltd. (Canada)
// Patent(s) Pending — All Rights Reserved — Applied Physics Division
//
// TL-Sponge-385 — Standalone Post-Quantum Sponge Construction
// Location: ternary-math/src/sponge.rs
//
// Squeeze modes:
//   squeeze()      — rate=243, capacity=486 (385-bit). Hashing, MAC, key derivation.
//   squeeze_bulk() — rate=486, capacity=243 (192-bit). Keystream. 2x throughput.

const STATE_SIZE: usize = 729;
const RATE: usize = 243;
const RATE_BULK: usize = 486;
const ROUNDS: usize = 9;
const LANES: usize = 27;
const CHI_BLOCKS: usize = 243;
pub const SPONGE_VERSION: u8 = 2;

#[inline(always)] fn balanced_wrap(s: i8) -> i8 { if s>=2{s-3}else if s<=-2{s+3}else{s} }
#[inline(always)] fn trit_add(a: i8, b: i8) -> i8 { let s=a+b; if s>1{s-3}else if s<-1{s+3}else{s} }
#[inline(always)] const fn gf3_mul(a:u8,b:u8)->u8{(a*b)%3}
#[inline(always)] const fn gf3_add(a:u8,b:u8)->u8{(a+b)%3}
#[inline(always)] const fn gf27_mul(a:[u8;3],b:[u8;3])->[u8;3]{
    let c0=gf3_mul(a[0],b[0]); let c1=gf3_add(gf3_mul(a[0],b[1]),gf3_mul(a[1],b[0]));
    let c2=gf3_add(gf3_add(gf3_mul(a[0],b[2]),gf3_mul(a[1],b[1])),gf3_mul(a[2],b[0]));
    let c3=gf3_add(gf3_mul(a[1],b[2]),gf3_mul(a[2],b[1])); let c4=gf3_mul(a[2],b[2]);
    [gf3_add(c0,gf3_mul(2,c3)),gf3_add(gf3_add(c1,c3),gf3_mul(2,c4)),gf3_add(c2,c4)]
}
#[inline(always)] const fn gf27_pow17(x:[u8;3])->[u8;3]{
    let x2=gf27_mul(x,x);let x4=gf27_mul(x2,x2);let x8=gf27_mul(x4,x4);let x16=gf27_mul(x8,x8);gf27_mul(x16,x)
}

static CHI_MAP: [[i8;3];27] = {
    let mut m=[[0i8;3];27]; let mut i=0;
    while i<27 { let[r0,r1,r2]=gf27_pow17([(i%3)as u8,((i/3)%3)as u8,(i/9)as u8]);
        m[i]=[r0 as i8-1,r1 as i8-1,r2 as i8-1]; i+=1; } m
};
static CHI_MAP_T0:[i8;32]={let mut t=[0i8;32];let mut i=0;while i<27{t[i]=CHI_MAP[i][0];i+=1;}t};
static CHI_MAP_T1:[i8;32]={let mut t=[0i8;32];let mut i=0;while i<27{t[i]=CHI_MAP[i][1];i+=1;}t};
static CHI_MAP_T2:[i8;32]={let mut t=[0i8;32];let mut i=0;while i<27{t[i]=CHI_MAP[i][2];i+=1;}t};

struct ThetaNeighbors{left:[u16;3],right:[u16;3]}
static THETA_IDX:[ThetaNeighbors;STATE_SIZE]={
    let mut t=[ThetaNeighbors{left:[0;3],right:[0;3]};STATE_SIZE];let w=STATE_SIZE;let mut i=0;
    while i<w{t[i]=ThetaNeighbors{
        left:[((i+w-13)%w)as u16,((i+w-7)%w)as u16,((i+w-1)%w)as u16],
        right:[((i+1)%w)as u16,((i+7)%w)as u16,((i+13)%w)as u16]};i+=1;}t
};
static PERM:[u16;STATE_SIZE]={let mut p=[0u16;STATE_SIZE];let mut i=0;while i<STATE_SIZE{p[i]=((i*376+1)%STATE_SIZE)as u16;i+=1;}p};
static RC_TABLE:[[i8;LANES];ROUNDS]={let mut rc=[[0i8;LANES];ROUNDS];let mut r=0;
    while r<ROUNDS{let mut l=0;while l<LANES{rc[r][l]=(r*7+l*13+3)as i8%3-1;l+=1;}r+=1;}rc};

fn chi_layer(state:&mut[i8;STATE_SIZE]){let mut b=0;while b<STATE_SIZE{
    let idx=(state[b]+1)as usize+(state[b+1]+1)as usize*3+(state[b+2]+1)as usize*9;
    let r=CHI_MAP[idx];state[b]=r[0];state[b+1]=r[1];state[b+2]=r[2];b+=3;}}

#[cfg(target_arch="x86_64")]
#[target_feature(enable="avx2")]
unsafe fn chi_layer_avx2(state:&mut[i8;STATE_SIZE]){
    use std::arch::x86_64::*;
    let t0l=_mm256_broadcastsi128_si256(_mm_loadu_si128(CHI_MAP_T0.as_ptr()as*const __m128i));
    let t0h=_mm256_broadcastsi128_si256(_mm_loadu_si128(CHI_MAP_T0.as_ptr().add(16)as*const __m128i));
    let t1l=_mm256_broadcastsi128_si256(_mm_loadu_si128(CHI_MAP_T1.as_ptr()as*const __m128i));
    let t1h=_mm256_broadcastsi128_si256(_mm_loadu_si128(CHI_MAP_T1.as_ptr().add(16)as*const __m128i));
    let t2l=_mm256_broadcastsi128_si256(_mm_loadu_si128(CHI_MAP_T2.as_ptr()as*const __m128i));
    let t2h=_mm256_broadcastsi128_si256(_mm_loadu_si128(CHI_MAP_T2.as_ptr().add(16)as*const __m128i));
    let v16=_mm256_set1_epi8(16);let v15=_mm256_set1_epi8(15);
    let mut indices=[0u8;CHI_BLOCKS];
    for b in 0..CHI_BLOCKS{let base=b*3;indices[b]=((state[base]+1)as u8)+((state[base+1]+1)as u8)*3+((state[base+2]+1)as u8)*9;}
    let mut i=0;
    while i+32<=CHI_BLOCKS{
        let iv=_mm256_loadu_si256(indices.as_ptr().add(i)as*const __m256i);
        let ih=_mm256_sub_epi8(iv,v16);let m=_mm256_cmpgt_epi8(iv,v15);
        let r0=_mm256_blendv_epi8(_mm256_shuffle_epi8(t0l,iv),_mm256_shuffle_epi8(t0h,ih),m);
        let r1=_mm256_blendv_epi8(_mm256_shuffle_epi8(t1l,iv),_mm256_shuffle_epi8(t1h,ih),m);
        let r2=_mm256_blendv_epi8(_mm256_shuffle_epi8(t2l,iv),_mm256_shuffle_epi8(t2h,ih),m);
        let mut o0=[0i8;32];let mut o1=[0i8;32];let mut o2=[0i8;32];
        _mm256_storeu_si256(o0.as_mut_ptr()as*mut __m256i,r0);
        _mm256_storeu_si256(o1.as_mut_ptr()as*mut __m256i,r1);
        _mm256_storeu_si256(o2.as_mut_ptr()as*mut __m256i,r2);
        for j in 0..32{let base=(i+j)*3;state[base]=o0[j];state[base+1]=o1[j];state[base+2]=o2[j];}
        i+=32;}
    while i<CHI_BLOCKS{let base=i*3;let idx=indices[i]as usize;let r=CHI_MAP[idx];state[base]=r[0];state[base+1]=r[1];state[base+2]=r[2];i+=1;}
}

#[cfg(target_arch="aarch64")]
#[target_feature(enable="neon")]
unsafe fn chi_layer_neon(state:&mut[i8;STATE_SIZE]){
    use std::arch::aarch64::*;
    let mt0=vld1q_s8(CHI_MAP_T0.as_ptr());let mt1=vld1q_s8(CHI_MAP_T1.as_ptr());let mt2=vld1q_s8(CHI_MAP_T2.as_ptr());
    let mt0h=vld1q_s8(CHI_MAP_T0.as_ptr().add(16));let mt1h=vld1q_s8(CHI_MAP_T1.as_ptr().add(16));let mt2h=vld1q_s8(CHI_MAP_T2.as_ptr().add(16));
    let mut indices=[0u8;CHI_BLOCKS];
    for b in 0..CHI_BLOCKS{let base=b*3;indices[b]=((state[base]+1)as u8)+((state[base+1]+1)as u8)*3+((state[base+2]+1)as u8)*9;}
    let v16=vdupq_n_u8(16);let mut i=0;
    while i+16<=CHI_BLOCKS{
        let iv=vld1q_u8(indices.as_ptr().add(i));let lm=vcltq_u8(iv,v16);let hi=vsubq_u8(iv,v16);
        let r0=vbslq_s8(lm,vqtbl1q_s8(mt0,vreinterpretq_u8_s8(vreinterpretq_s8_u8(iv))),vqtbl1q_s8(mt0h,vreinterpretq_u8_s8(vreinterpretq_s8_u8(hi))));
        let r1=vbslq_s8(lm,vqtbl1q_s8(mt1,vreinterpretq_u8_s8(vreinterpretq_s8_u8(iv))),vqtbl1q_s8(mt1h,vreinterpretq_u8_s8(vreinterpretq_s8_u8(hi))));
        let r2=vbslq_s8(lm,vqtbl1q_s8(mt2,vreinterpretq_u8_s8(vreinterpretq_s8_u8(iv))),vqtbl1q_s8(mt2h,vreinterpretq_u8_s8(vreinterpretq_s8_u8(hi))));
        let mut o0=[0i8;16];let mut o1=[0i8;16];let mut o2=[0i8;16];
        vst1q_s8(o0.as_mut_ptr(),r0);vst1q_s8(o1.as_mut_ptr(),r1);vst1q_s8(o2.as_mut_ptr(),r2);
        for j in 0..16{let base=(i+j)*3;state[base]=o0[j];state[base+1]=o1[j];state[base+2]=o2[j];}i+=16;}
    while i<CHI_BLOCKS{let base=i*3;let idx=indices[i]as usize;let r=CHI_MAP[idx];state[base]=r[0];state[base+1]=r[1];state[base+2]=r[2];i+=1;}
}

fn theta_pi_rc(state:&mut[i8;STATE_SIZE],buf:&mut[i8;STATE_SIZE],round:usize){
    for i in 0..STATE_SIZE{let n=&THETA_IDX[i];
        let left=balanced_wrap(state[n.left[0]as usize]+state[n.left[1]as usize]+state[n.left[2]as usize]);
        let right=balanced_wrap(state[n.right[0]as usize]+state[n.right[1]as usize]+state[n.right[2]as usize]);
        buf[i]=balanced_wrap(left+state[i]+right+1);}
    for i in 0..STATE_SIZE{state[PERM[i]as usize]=buf[i];}
    let rc=&RC_TABLE[round];for lane in 0..LANES{let idx=lane*LANES;state[idx]=balanced_wrap(state[idx]+rc[lane]);}
}

fn sponge_permutation_v2_scalar(state:&mut[i8;STATE_SIZE]){let mut buf=[0i8;STATE_SIZE];for r in 0..ROUNDS{chi_layer(state);theta_pi_rc(state,&mut buf,r);}}
fn sponge_permutation_v1_scalar(state:&mut[i8;STATE_SIZE]){let mut buf=[0i8;STATE_SIZE];for r in 0..ROUNDS{theta_pi_rc(state,&mut buf,r);}}

#[cfg(target_arch="x86_64")]
#[target_feature(enable="avx2")]
unsafe fn sponge_permutation_v2_avx2(state:&mut[i8;STATE_SIZE]){
    use std::arch::x86_64::*;
    let mut ext=[0i8;STATE_SIZE+26];let mut buf=[0i8;STATE_SIZE];
    let v1=_mm256_set1_epi8(1);let vh=_mm256_set1_epi8(1);let vl=_mm256_set1_epi8(-1);let v3=_mm256_set1_epi8(3);
    for round in 0..ROUNDS{
        chi_layer_avx2(state);
        ext[..13].copy_from_slice(&state[STATE_SIZE-13..]);ext[13..13+STATE_SIZE].copy_from_slice(state);ext[13+STATE_SIZE..].copy_from_slice(&state[..13]);
        let mut i=0;
        while i+32<=STATE_SIZE{let ei=i+13;
            let ls=_mm256_add_epi8(_mm256_add_epi8(_mm256_loadu_si256(ext.as_ptr().add(ei-13)as*const __m256i),_mm256_loadu_si256(ext.as_ptr().add(ei-7)as*const __m256i)),_mm256_loadu_si256(ext.as_ptr().add(ei-1)as*const __m256i));
            let lw=_mm256_blendv_epi8(_mm256_blendv_epi8(ls,_mm256_sub_epi8(ls,v3),_mm256_cmpgt_epi8(ls,vh)),_mm256_add_epi8(ls,v3),_mm256_cmpgt_epi8(vl,ls));
            let rs=_mm256_add_epi8(_mm256_add_epi8(_mm256_loadu_si256(ext.as_ptr().add(ei+1)as*const __m256i),_mm256_loadu_si256(ext.as_ptr().add(ei+7)as*const __m256i)),_mm256_loadu_si256(ext.as_ptr().add(ei+13)as*const __m256i));
            let rw=_mm256_blendv_epi8(_mm256_blendv_epi8(rs,_mm256_sub_epi8(rs,v3),_mm256_cmpgt_epi8(rs,vh)),_mm256_add_epi8(rs,v3),_mm256_cmpgt_epi8(vl,rs));
            let t=_mm256_add_epi8(_mm256_add_epi8(_mm256_add_epi8(lw,_mm256_loadu_si256(ext.as_ptr().add(ei)as*const __m256i)),rw),v1);
            let res=_mm256_blendv_epi8(_mm256_blendv_epi8(t,_mm256_sub_epi8(t,v3),_mm256_cmpgt_epi8(t,vh)),_mm256_add_epi8(t,v3),_mm256_cmpgt_epi8(vl,t));
            _mm256_storeu_si256(buf.as_mut_ptr().add(i)as*mut __m256i,res);i+=32;}
        while i<STATE_SIZE{let ei=i+13;buf[i]=balanced_wrap(balanced_wrap(ext[ei-13]+ext[ei-7]+ext[ei-1])+ext[ei]+balanced_wrap(ext[ei+1]+ext[ei+7]+ext[ei+13])+1);i+=1;}
        for i in 0..STATE_SIZE{state[PERM[i]as usize]=buf[i];}
        let rc=&RC_TABLE[round];for lane in 0..LANES{let idx=lane*LANES;state[idx]=balanced_wrap(state[idx]+rc[lane]);}}}

#[cfg(target_arch="x86_64")]
#[target_feature(enable="avx2")]
unsafe fn sponge_permutation_v1_avx2(state:&mut[i8;STATE_SIZE]){
    use std::arch::x86_64::*;
    let mut ext=[0i8;STATE_SIZE+26];let mut buf=[0i8;STATE_SIZE];
    let v1=_mm256_set1_epi8(1);let vh=_mm256_set1_epi8(1);let vl=_mm256_set1_epi8(-1);let v3=_mm256_set1_epi8(3);
    for round in 0..ROUNDS{
        ext[..13].copy_from_slice(&state[STATE_SIZE-13..]);ext[13..13+STATE_SIZE].copy_from_slice(state);ext[13+STATE_SIZE..].copy_from_slice(&state[..13]);
        let mut i=0;
        while i+32<=STATE_SIZE{let ei=i+13;
            let ls=_mm256_add_epi8(_mm256_add_epi8(_mm256_loadu_si256(ext.as_ptr().add(ei-13)as*const __m256i),_mm256_loadu_si256(ext.as_ptr().add(ei-7)as*const __m256i)),_mm256_loadu_si256(ext.as_ptr().add(ei-1)as*const __m256i));
            let lw=_mm256_blendv_epi8(_mm256_blendv_epi8(ls,_mm256_sub_epi8(ls,v3),_mm256_cmpgt_epi8(ls,vh)),_mm256_add_epi8(ls,v3),_mm256_cmpgt_epi8(vl,ls));
            let rs=_mm256_add_epi8(_mm256_add_epi8(_mm256_loadu_si256(ext.as_ptr().add(ei+1)as*const __m256i),_mm256_loadu_si256(ext.as_ptr().add(ei+7)as*const __m256i)),_mm256_loadu_si256(ext.as_ptr().add(ei+13)as*const __m256i));
            let rw=_mm256_blendv_epi8(_mm256_blendv_epi8(rs,_mm256_sub_epi8(rs,v3),_mm256_cmpgt_epi8(rs,vh)),_mm256_add_epi8(rs,v3),_mm256_cmpgt_epi8(vl,rs));
            let t=_mm256_add_epi8(_mm256_add_epi8(_mm256_add_epi8(lw,_mm256_loadu_si256(ext.as_ptr().add(ei)as*const __m256i)),rw),v1);
            let res=_mm256_blendv_epi8(_mm256_blendv_epi8(t,_mm256_sub_epi8(t,v3),_mm256_cmpgt_epi8(t,vh)),_mm256_add_epi8(t,v3),_mm256_cmpgt_epi8(vl,t));
            _mm256_storeu_si256(buf.as_mut_ptr().add(i)as*mut __m256i,res);i+=32;}
        while i<STATE_SIZE{let ei=i+13;buf[i]=balanced_wrap(balanced_wrap(ext[ei-13]+ext[ei-7]+ext[ei-1])+ext[ei]+balanced_wrap(ext[ei+1]+ext[ei+7]+ext[ei+13])+1);i+=1;}
        for i in 0..STATE_SIZE{state[PERM[i]as usize]=buf[i];}
        let rc=&RC_TABLE[round];for lane in 0..LANES{let idx=lane*LANES;state[idx]=balanced_wrap(state[idx]+rc[lane]);}}}

#[cfg(target_arch="aarch64")]
#[target_feature(enable="neon")]
unsafe fn sponge_permutation_v2_neon(state:&mut[i8;STATE_SIZE]){
    use std::arch::aarch64::*;
    let mut ext=[0i8;STATE_SIZE+26];let mut buf=[0i8;STATE_SIZE];
    let v1=vdupq_n_s8(1);let vh=vdupq_n_s8(1);let vl=vdupq_n_s8(-1);let v3=vdupq_n_s8(3);let vn3=vdupq_n_s8(-3);
    for round in 0..ROUNDS{
        chi_layer_neon(state);
        ext[..13].copy_from_slice(&state[STATE_SIZE-13..]);ext[13..13+STATE_SIZE].copy_from_slice(state);ext[13+STATE_SIZE..].copy_from_slice(&state[..13]);
        let mut i=0;
        while i+16<=STATE_SIZE{let ei=i+13;
            let ls=vaddq_s8(vaddq_s8(vld1q_s8(ext.as_ptr().add(ei-13)),vld1q_s8(ext.as_ptr().add(ei-7))),vld1q_s8(ext.as_ptr().add(ei-1)));
            let lw=vbslq_s8(vcltq_s8(ls,vl),vaddq_s8(ls,v3),vbslq_s8(vcgtq_s8(ls,vh),vaddq_s8(ls,vn3),ls));
            let rs=vaddq_s8(vaddq_s8(vld1q_s8(ext.as_ptr().add(ei+1)),vld1q_s8(ext.as_ptr().add(ei+7))),vld1q_s8(ext.as_ptr().add(ei+13)));
            let rw=vbslq_s8(vcltq_s8(rs,vl),vaddq_s8(rs,v3),vbslq_s8(vcgtq_s8(rs,vh),vaddq_s8(rs,vn3),rs));
            let t=vaddq_s8(vaddq_s8(vaddq_s8(lw,vld1q_s8(ext.as_ptr().add(ei))),rw),v1);
            let res=vbslq_s8(vcltq_s8(t,vl),vaddq_s8(t,v3),vbslq_s8(vcgtq_s8(t,vh),vaddq_s8(t,vn3),t));
            vst1q_s8(buf.as_mut_ptr().add(i),res);i+=16;}
        while i<STATE_SIZE{let ei=i+13;buf[i]=balanced_wrap(balanced_wrap(ext[ei-13]+ext[ei-7]+ext[ei-1])+ext[ei]+balanced_wrap(ext[ei+1]+ext[ei+7]+ext[ei+13])+1);i+=1;}
        for i in 0..STATE_SIZE{state[PERM[i]as usize]=buf[i];}
        let rc=&RC_TABLE[round];for lane in 0..LANES{let idx=lane*LANES;state[idx]=balanced_wrap(state[idx]+rc[lane]);}}}

#[cfg(target_arch="aarch64")]
#[target_feature(enable="neon")]
unsafe fn sponge_permutation_v1_neon(state:&mut[i8;STATE_SIZE]){
    use std::arch::aarch64::*;
    let mut ext=[0i8;STATE_SIZE+26];let mut buf=[0i8;STATE_SIZE];
    let v1=vdupq_n_s8(1);let vh=vdupq_n_s8(1);let vl=vdupq_n_s8(-1);let v3=vdupq_n_s8(3);let vn3=vdupq_n_s8(-3);
    for round in 0..ROUNDS{
        ext[..13].copy_from_slice(&state[STATE_SIZE-13..]);ext[13..13+STATE_SIZE].copy_from_slice(state);ext[13+STATE_SIZE..].copy_from_slice(&state[..13]);
        let mut i=0;
        while i+16<=STATE_SIZE{let ei=i+13;
            let ls=vaddq_s8(vaddq_s8(vld1q_s8(ext.as_ptr().add(ei-13)),vld1q_s8(ext.as_ptr().add(ei-7))),vld1q_s8(ext.as_ptr().add(ei-1)));
            let lw=vbslq_s8(vcltq_s8(ls,vl),vaddq_s8(ls,v3),vbslq_s8(vcgtq_s8(ls,vh),vaddq_s8(ls,vn3),ls));
            let rs=vaddq_s8(vaddq_s8(vld1q_s8(ext.as_ptr().add(ei+1)),vld1q_s8(ext.as_ptr().add(ei+7))),vld1q_s8(ext.as_ptr().add(ei+13)));
            let rw=vbslq_s8(vcltq_s8(rs,vl),vaddq_s8(rs,v3),vbslq_s8(vcgtq_s8(rs,vh),vaddq_s8(rs,vn3),rs));
            let t=vaddq_s8(vaddq_s8(vaddq_s8(lw,vld1q_s8(ext.as_ptr().add(ei))),rw),v1);
            let res=vbslq_s8(vcltq_s8(t,vl),vaddq_s8(t,v3),vbslq_s8(vcgtq_s8(t,vh),vaddq_s8(t,vn3),t));
            vst1q_s8(buf.as_mut_ptr().add(i),res);i+=16;}
        while i<STATE_SIZE{let ei=i+13;buf[i]=balanced_wrap(balanced_wrap(ext[ei-13]+ext[ei-7]+ext[ei-1])+ext[ei]+balanced_wrap(ext[ei+1]+ext[ei+7]+ext[ei+13])+1);i+=1;}
        for i in 0..STATE_SIZE{state[PERM[i]as usize]=buf[i];}
        let rc=&RC_TABLE[round];for lane in 0..LANES{let idx=lane*LANES;state[idx]=balanced_wrap(state[idx]+rc[lane]);}}}

pub fn sponge_permutation(state:&mut[i8;STATE_SIZE]){
    #[cfg(target_arch="x86_64")]{if is_x86_feature_detected!("avx2"){unsafe{sponge_permutation_v2_avx2(state);}return;}}
    #[cfg(target_arch="aarch64")]{if std::arch::is_aarch64_feature_detected!("neon"){unsafe{sponge_permutation_v2_neon(state);}return;}}
    sponge_permutation_v2_scalar(state);
}
pub fn sponge_permutation_v1(state:&mut[i8;STATE_SIZE]){
    #[cfg(target_arch="x86_64")]{if is_x86_feature_detected!("avx2"){unsafe{sponge_permutation_v1_avx2(state);}return;}}
    #[cfg(target_arch="aarch64")]{if std::arch::is_aarch64_feature_detected!("neon"){unsafe{sponge_permutation_v1_neon(state);}return;}}
    sponge_permutation_v1_scalar(state);
}

pub fn bytes_to_trits_pub(bytes:&[u8])->Vec<i8>{bytes_to_trits(bytes)}
fn bytes_to_trits(bytes:&[u8])->Vec<i8>{let mut t=Vec::with_capacity(bytes.len()*5);for &b in bytes{let mut v=b;for _ in 0..5{t.push((v%3)as i8-1);v/=3;}}t}
fn trits_to_bytes(trits:&[i8])->Vec<u8>{let mut b=Vec::with_capacity((trits.len()+4)/5);let mut i=0;
    while i<trits.len(){let mut v:u8=0;let mut p:u8=1;for j in 0..5{if i+j<trits.len(){v+=(trits[i+j]+1)as u8*p;}p=p.wrapping_mul(3);}b.push(v);i+=5;}b}

#[derive(Clone)]
pub struct Sponge385Pub{state:[i8;STATE_SIZE],buf:[i8;RATE],buf_len:usize,absorbed:bool,use_v1:bool}
impl Sponge385Pub{
    pub fn new()->Self{Self{state:[0;STATE_SIZE],buf:[0;RATE],buf_len:0,absorbed:false,use_v1:false}}
    pub fn new_v1()->Self{Self{state:[0;STATE_SIZE],buf:[0;RATE],buf_len:0,absorbed:false,use_v1:true}}
    fn permute(&mut self){if self.use_v1{sponge_permutation_v1(&mut self.state);}else{sponge_permutation(&mut self.state);}}
    pub fn absorb(&mut self,input:&[i8]){
        if input.is_empty(){return;}self.absorbed=true;let mut off=0;let len=input.len();
        if self.buf_len>0{let fill=len.min(RATE-self.buf_len);self.buf[self.buf_len..self.buf_len+fill].copy_from_slice(&input[..fill]);
            self.buf_len+=fill;off=fill;if self.buf_len==RATE{for i in 0..RATE{self.state[i]=trit_add(self.state[i],self.buf[i]);}self.permute();self.buf_len=0;}}
        while off+RATE<=len{for i in 0..RATE{self.state[i]=trit_add(self.state[i],input[off+i]);}self.permute();off+=RATE;}
        let rem=len-off;if rem>0{self.buf[self.buf_len..self.buf_len+rem].copy_from_slice(&input[off..]);self.buf_len+=rem;}}
    pub fn absorb_bytes(&mut self,input:&[u8]){self.absorb(&bytes_to_trits(input));}
    fn finalize_absorb(&mut self){for i in 0..self.buf_len{self.state[i]=trit_add(self.state[i],self.buf[i]);}
        if self.buf_len<RATE{self.state[self.buf_len]=trit_add(self.state[self.buf_len],1);}self.buf_len=0;self.permute();}
    pub fn squeeze(&mut self,n:usize)->Vec<i8>{if self.buf_len>0||!self.absorbed{self.finalize_absorb();}
        let mut out=Vec::with_capacity(n);while out.len()<n{let take=(n-out.len()).min(RATE);out.extend_from_slice(&self.state[..take]);if out.len()<n{self.permute();}}out.truncate(n);out}
    pub fn squeeze_bulk(&mut self,n:usize)->Vec<i8>{if self.buf_len>0||!self.absorbed{self.finalize_absorb();}
        let mut out=Vec::with_capacity(n);while out.len()<n{let take=(n-out.len()).min(RATE_BULK);out.extend_from_slice(&self.state[..take]);if out.len()<n{self.permute();}}out.truncate(n);out}
}

pub fn hash(input:&[u8],output_len:usize)->Vec<u8>{let mut s=Sponge385Pub::new();s.absorb_bytes(input);trits_to_bytes(&s.squeeze(output_len*5))[..output_len].to_vec()}
pub fn hash_v1(input:&[u8],output_len:usize)->Vec<u8>{let mut s=Sponge385Pub::new_v1();s.absorb_bytes(input);trits_to_bytes(&s.squeeze(output_len*5))[..output_len].to_vec()}
pub fn hash_hex(input:&[u8])->String{let mut s=Sponge385Pub::new();s.absorb_bytes(input);trits_to_bytes(&s.squeeze(243))[..49].iter().map(|b|format!("{:02x}",b)).collect()}
pub fn hash_hex_v1(input:&[u8])->String{let mut s=Sponge385Pub::new_v1();s.absorb_bytes(input);trits_to_bytes(&s.squeeze(243))[..49].iter().map(|b|format!("{:02x}",b)).collect()}
pub fn derive_key(context:&[u8],material:&[u8],key_len:usize)->Vec<u8>{let mut v=Vec::with_capacity(context.len()+material.len());v.extend_from_slice(context);v.extend_from_slice(material);hash(&v,key_len)}
pub fn sponge385_derive_key(domain:&[u8],a:&[u8],b:&[u8],secret:&[u8;32],epoch:u64)->Vec<u8>{
    let mut s=Sponge385Pub::new();s.absorb_bytes(domain);s.absorb_bytes(a);s.absorb_bytes(b);
    s.absorb_bytes(secret);s.absorb_bytes(&epoch.to_le_bytes());trits_to_bytes(&s.squeeze(RATE))[..32].to_vec()}

#[cfg(test)]
mod tests{use super::*;
    #[test]fn test_hash_det(){assert_eq!(hash(b"hello world",32),hash(b"hello world",32));}
    #[test]fn test_hash_diff(){assert_ne!(hash(b"hello",32),hash(b"world",32));}
    #[test]fn test_dk_det(){assert_eq!(derive_key(b"c",b"m",32),derive_key(b"c",b"m",32));}
    #[test]fn test_dk_len(){assert_eq!(derive_key(b"c",b"m",32).len(),32);}
    #[test]fn test_s385dk(){let k1=sponge385_derive_key(b"D",b"a",b"b",&[42;32],100);let k2=sponge385_derive_key(b"D",b"a",b"b",&[42;32],100);assert_eq!(k1,k2);assert_eq!(k1.len(),32);}
    #[test]fn test_s385dk_ds(){assert_ne!(sponge385_derive_key(b"D",b"a",b"b",&[42;32],100),sponge385_derive_key(b"D",b"a",b"b",&[99;32],100));}
    #[test]fn test_s385dk_de(){assert_ne!(sponge385_derive_key(b"D",b"a",b"b",&[42;32],100),sponge385_derive_key(b"D",b"a",b"b",&[42;32],200));}
    #[test]fn test_perm(){let mut seen=[false;STATE_SIZE];for i in 0..STATE_SIZE{let d=PERM[i]as usize;assert!(!seen[d]);seen[d]=true;}}
    #[test]fn test_coprime(){fn gcd(mut a:usize,mut b:usize)->usize{while b!=0{let t=b;b=a%b;a=t;}a}assert_eq!(gcd(1,STATE_SIZE),1);assert_eq!(gcd(7,STATE_SIZE),1);assert_eq!(gcd(13,STATE_SIZE),1);}
    #[test]fn test_chi_bij(){let mut o=Vec::new();for a0 in 0u8..3{for a1 in 0u8..3{for a2 in 0u8..3{o.push(gf27_pow17([a0,a1,a2]));}}}assert_eq!(gf27_pow17([0,0,0]),[0,0,0]);o.sort();o.dedup();assert_eq!(o.len(),27);}
    #[test]fn test_v2_diff_v1(){let mut a=[0i8;STATE_SIZE];let mut b=[0i8;STATE_SIZE];a[0]=1;a[1]=-1;a[3]=1;b=a;sponge_permutation_v1(&mut a);sponge_permutation(&mut b);assert_ne!(a,b);}
    #[test]fn test_simd_v2(){let mut a=[0i8;STATE_SIZE];let mut b=[0i8;STATE_SIZE];for i in 0..STATE_SIZE{let v=((i*7+3)%3)as i8-1;a[i]=v;b[i]=v;}sponge_permutation(&mut a);sponge_permutation_v2_scalar(&mut b);assert_eq!(a,b);}
    #[test]fn test_simd_v1(){let mut a=[0i8;STATE_SIZE];let mut b=[0i8;STATE_SIZE];for i in 0..STATE_SIZE{let v=((i*7+3)%3)as i8-1;a[i]=v;b[i]=v;}sponge_permutation_v1(&mut a);sponge_permutation_v1_scalar(&mut b);assert_eq!(a,b);}
    #[test]fn test_clone(){let mut s1=Sponge385Pub::new();s1.absorb_bytes(b"test");let mut s2=s1.clone();assert_eq!(s1.squeeze(500),s2.squeeze(500));}
    #[test]fn test_bulk(){let mut s1=Sponge385Pub::new();s1.absorb_bytes(b"bulk");let mut s2=Sponge385Pub::new();s2.absorb_bytes(b"bulk");
        let a=s1.squeeze(1000);let b=s2.squeeze_bulk(1000);assert_eq!(&a[..RATE],&b[..RATE]);assert_ne!(&a[RATE..],&b[RATE..]);}
}