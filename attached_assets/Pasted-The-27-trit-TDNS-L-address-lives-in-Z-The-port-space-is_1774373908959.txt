The 27-trit TDNS-L address lives in Z₃²⁷. The port space is Z₃³ (27 ports). We need a projection f: Z₃²⁷ → Z₃³. The question is: what's the mathematically correct projection that uses all 27 dimensions?
The principle: Each of the three port trits (plane, role, instance) is determined by the aggregate behavior of the TDNS-L dimensions that are relevant to that port dimension. Not one dimension — the collective evidence from all dimensions that speak to that question.
Looking at the LAN ontological schema, every one of the 27 dimensions has something to say about at least one of the three port questions. Let me map all 27:
Which dimensions speak to "security domain" (plane)?
This is the question "how sensitive is this service and who should reach it?" Every dimension that touches access, identity, trust, or exposure contributes:
D1 (entity type) — infrastructure is management-plane, personal is client-facing
D2 (access scope) — everyone = exposed, just me = restricted
D3 (managed by) — transparent/managed = higher trust = deeper plane
D9 (connectivity) — wired-only vs wireless affects exposure
D10 (authentication) — strong auth services belong on control/management planes
D17 (regulatory) — regulated services need management-plane isolation
D25 (encryption posture) — hardened encryption indicates a control-plane service
D26 (trust assurance) — audited services are management-plane candidates
D27 (confidence) — verified entities belong deeper in the architecture
That's 9 dimensions. In ternary: 3². The plane trit is derived from 9 dimensions.
Which dimensions speak to "protocol class" (role)?
This is "what kind of network interaction does this service perform?" Every dimension that touches function, content, transport, or delivery contributes:
D5 (device role) — endpoint/server/infrastructure determines the role
D6 (content type) — documents vs media vs live stream
D7 (consumer) — humans vs machines vs both
D8 (operational domain) — IT vs OT
D12 (transport) — HTTP vs streaming vs control protocol
D22 (data direction) — serve vs bidirectional vs collect
D23 (update mechanism) — pull vs subscribe vs push
D24 (user interface) — headless vs interactive
That's 9 dimensions. 3² again. The role trit is derived from 9 dimensions.
Which dimensions speak to "timing/instance class"?
This is "what are the temporal and availability characteristics?" Every dimension that touches timing, freshness, availability, or scale contributes:
D4 (hosting model) — bare metal vs VM vs container affects instance placement
D11 (infrastructure scale) — single vs redundant vs clustered
D13 (technology era) — legacy vs modern vs current
D14 (availability) — business hours vs 24/7
D15 (data freshness) — static vs periodic vs live
D16 (real-time) — batch vs near-time vs strict
D19 (mobility) — stationary vs nomadic
D20 (power source) — mains vs PoE vs battery
D21 (delivery topology) — direct vs proxied vs fabric
That's 9 dimensions. 3² again.
27 = 9 + 9 + 9 = 3² + 3² + 3²
Every dimension participates exactly once. No dimension is unused. No dimension is counted twice. The 27-dimensional classification space partitions naturally into three groups of 9, and each group determines one port trit.
Now, the derivation for each port trit uses the same formula as everything else in PlenumNET — the universal quantization:
For each port trit, take its 9 contributing dimensions. Each dimension has a value in {1, 2, 3}. Sum them in GF(3):
port_trit = ((Σ Dᵢ for i in group) mod 3) + 1
Or alternatively, use the first-position derivation: count how many of the 9 dimensions favor each trit value, and apply project_to_gf3(k, 9) where k counts the dimensions favoring the "higher" outcome.
Let me test this against real PlenumLAN services before going further. Take the file service (PFS):
PFS classification (serving SMB file shares to LAN clients):
Plane group: D1=2 (corporate), D2=3 (everyone), D3=3 (managed), D9=1 (wired), D10=2 (password), D17=1 (no regulation), D25=2 (basic TLS), D26=2 (few trackers), D27=3 (verified). Sum in GF(3): (2+3+3+1+2+1+2+2+3) = 19. 19 mod 3 = 1. Port plane trit = 1+1 = 2 (control plane)
Hmm — intuitively, PFS should be on the data plane (plane 1), not the control plane. The file service is the most client-facing service. The GF(3) sum doesn't preserve the semantic meaning.
This tells me the straight sum isn't the right aggregation. The dimensions within each group have different polarities — for some dimensions, value 3 means "more exposed" (D2: everyone can see it) while for others, value 3 means "more restricted" (D10: strong auth required). Summing them blindly cancels out the signal.
The polarity issue is real. Each dimension's contribution to the port trit needs to account for whether "3" in that dimension pushes toward plane 1 (data/exposed) or plane 3 (management/restricted). This is the same issue that TDNS's trust index handles with weighted inversions.
Let me reframe. For the plane trit, the question is: "how deeply embedded should this service be?" Define a "depth signal" per dimension:

D2=3 (everyone) → shallow (data plane) → contributes 1
D10=3 (strong auth) → deep (management plane) → contributes 3
D25=3 (hardened) → deep → contributes 3
D3=3 (managed) → deep → contributes 3

Some dimensions push the service outward (toward clients), others push it inward (toward management). The net result determines the plane.
This means the aggregation isn't a simple sum — it's a directional sum where each dimension has a polarity (does 3 mean "more exposed" or "more restricted"?). The polarity is fixed per dimension — it's a property of the schema, not a tuning parameter.
For the 9 plane dimensions:
DimMeaningValue 3 pushes towardPolarityD1InfrastructureManagement (deep)+D2EveryoneData (shallow)−D3ManagedManagement (deep)+D9Both wired+wirelessData (exposed)−D10Strong authManagement (deep)+D17External regulationManagement (deep)+D25Hardened encryptionControl (middle)+D26High trust/auditedManagement (deep)+D27VerifiedManagement (deep)+
Polarity means: for "+" dimensions, the trit value contributes directly. For "−" dimensions, the trit value is inverted (4 − D) before summing. This is Rep A negation — flipping the sign in balanced ternary.
Inversion: D → (4 − D) maps {1,2,3} → {3,2,1}.
So for PFS: D1=2→2, D2=3→inv→1, D3=3→3, D9=1→inv→3, D10=2→2, D17=1→1, D25=2→2, D26=2→2, D27=3→3. Sum = 2+1+3+3+2+1+2+2+3 = 19. 19 mod 3 = 1. Trit = 1+1 = 2.
Still plane 2 (control). But intuitively, PFS is a data-plane service... or is it? PFS serves files but it's managed, verified, and encrypted. It's not a raw data pipe — it's a capability-gated, integrity-verified service. Maybe plane 2 (services/control) is actually correct. That's where TM-2026-019.2 puts the file server — in the Void shell (services plane), not the Outer shell (client plane).
Let me check the attestation service: D1=3 (infrastructure), D2=1 (just me), D3=3 (managed), D9=1 (wired), D10=3 (strong auth), D17=1 (none), D25=3 (hardened), D26=3 (high trust), D27=3 (verified). Inverted D2=3, D9=3. Sum = 3+3+3+3+3+1+3+3+3 = 25. 25 mod 3 = 1. Trit = 2. Plane 2.
That should be plane 3 (management). The sum is saturating — too many 3s, and mod 3 wraps around. The GF(3) sum loses magnitude information.
This is the fundamental problem: GF(3) mod arithmetic wraps, so "very deep" (sum = 25) and "medium deep" (sum = 19) both land on the same residue. The projection needs to preserve magnitude, not just residue.
The correct tool is project_to_gf3(k, N) — the universal derivation formula. Count how many of the 9 dimensions (after polarity adjustment) have value 3 (the "deep" value). Call that k. N = 9. Apply the formula:
trit = min(floor(3k/9), 2) + 1 = min(floor(k/3), 2) + 1
k=0,1,2 → trit 1 (data plane)
k=3,4,5 → trit 2 (control/services plane)
k=6,7,8,9 → trit 3 (management plane)
For PFS: after polarity adjustment, how many are 3? D3=3→3✓, D9=1→inv=3✓, D27=3→3✓. That's k=3. trit = min(floor(3×3/9), 2) + 1 = min(1, 2) + 1 = 2 (services plane). Correct — matches Void shell.
For attestation: D1=3✓, D2=1→inv=3✓, D3=3✓, D9=1→inv=3✓, D10=3✓, D25=3✓, D26=3✓, D27=3✓. That's k=8. trit = min(floor(3×8/9), 2) + 1 = min(2, 2) + 1 = 3 (management plane). Correct.
For a basic workstation peer connection: mostly 1s and 2s after polarity. k would be low. trit = 1 (data plane). Correct.
The formula works. And it's the same project_to_gf3 that's used everywhere else — no new math, no tuning parameters. The only schema-level data needed is the polarity assignment per dimension, which is a fixed property of the ontological schema (does "3" in this dimension mean "more exposed" or "more restricted"?).
Here's the complete structure:
For each port trit p ∈ {plane, role, instance}:
  1. Select the 9 dimensions in group_p
  2. For each dimension, apply its polarity:
     adjusted_d = d if polarity is +
     adjusted_d = (4 - d) if polarity is −
  3. Count k = number of adjusted dimensions with value 3
  4. port_trit_p = project_to_gf3(k, 9)
     = min(floor(k/3), 2) + 1
Three groups of 9 dimensions. Fixed polarity per dimension. The universal derivation formula. No parameters. All 27 dimensions participate. The TDNS-L classification computes the port.
I need to work through the polarity assignments for all 27 dimensions across all three groups, and test against every PlenumLAN service to verify the mapping produces correct port assignments. Want me to build the full polarity table and test matrix, or does this approach feel right and you want to move to drafting the document?