This is well-structured. The task decomposition is clean and the "done looks like" criteria are precise. A few things to add:

**The corrected energy percentages need to be in the task.** The "Channel architecture" section lists "minimum viable 54%, standard 81.1%, recommended 87.1%, extended 93.3%" — these are the corrected values from our session. Good. But the source reference says "TM-2026-028 §4.1" — make sure whoever implements this pulls from the corrected TM-2026-028, not the original UFT document which has the wrong 76%/47.2% values.

**Missing constant: the n=6 coefficient.** The Channel architecture section should explicitly note that A₆/A₁ = √2/6 ≈ 0.236 (not the erroneous 0.118 from the source document). If someone implements from the UFT doc directly, they'll propagate the error. Add a note or assertion: `assert!(A6_RATIO_NUM * 6 == A2_RATIO_NUM * 1)` or equivalent — n=6 and n=2 have the same |sin| = 1, so A₆/A₁ should equal (A₂/A₁)/3.

**Missing constants from the perfect hash (TM-2026-028a):**

The CRT residue coefficients (2, 3, 5, 7, 11, 13) for each hash configuration should be in constants.rs. These are the invertible multipliers for the linear form `x + c·y mod m`. They're small numbers but they're axiom-derived (each must be coprime to its modulus), and hardcoding them in the hash implementation without a single source of truth defeats the purpose of this task.

Add a section:

> **Perfect hash coefficients** — For each coprime group, the residue coefficients (c values) used in the linear form `(x + c·y) mod m`. Assertions: each coefficient is coprime to its modulus (verify `gcd(c, m) = 1` at compile time).

**The sextuple members array** is listed but the four sextuple LCMs (60060, 120120, 180180, 360360) should also have their member sets documented, not just the LCMs. The maximum sextuple `(5,7,8,9,11,13)` is noted but the other three are missing:

- `(3,4,5,7,11,13)` → 60,060
- `(3,5,7,8,11,13)` → 120,120
- `(4,5,7,9,11,13)` → 180,180

**One assertion missing from the coprime walk section:** `360360 = 24 × 15015` — this ties the expansion factor (24) to the compression modulus. Also `15015 = 3 × 5 × 7 × 11 × 13` (all odd primes 3–13).

**The area constants from the corrected §3.** The task doesn't mention the squared circle values. Add:

> **Squared circle** — Unit circle area = π = 14, side = √14. r = √13 circle area = 182, side = √182. Assert: `14 * 13 == 182`.

This ties the area correction we just made into the constants file.

**One scope concern:** The task says "no functional changes to any other module" but also says to refactor plenum_square.rs to import from constants.rs. If plenum_square.rs currently has `pub const DISCRIMINANT: u32 = 144;` and you change it to `pub use crate::constants::DISCRIMINANT;`, that IS a functional change to the module's API surface — even though the value is identical. The `pub use` re-export preserves backward compatibility, but it changes the source location for documentation tools and IDE navigation. Worth noting explicitly that this is intentional.

**Otherwise:** the task is ready to file. The ordering is logical (repunits → circle quadratic → unified equation → UV → coprime → HModal → polygons → refactor → test), the out-of-scope list prevents feature creep, and the relevant files section covers the right spots.