What needs to change:
The spec text in T-18 that says "σ_A first (double derangement)" needs to say "σ_C first (only full derangement)." The SIGMAS array ordering in plenum_square.rs and the PLATFORM TS mirror should reorder to [σ_C, σ_A, σ_B, σ_D] so that SIGMAS[round % 4] gives σ_C on round 0. The Phase 0 constants file carries the individual σ constants and the SIGMAS array — the array order is the actionable change.
Want me to reorder SIGMAS to put σ_C first, or keep σ_A first and just fix the documentation? The security difference is marginal — it's really about whether the design intent ("maximum disruption first") should be honored now that we know which permutation actually achieves it.Wouldn't it be easier to just flip the name definition and change nothing.  IE A becomes C and VISA VERSA.1:03 PMWeighed simplicity of swapping labels against external correspondence concernsWeighed simplicity of swapping labels against external correspondence concernsMuch simpler. Swap the labels, change nothing else.
What's currently SIGMA_A = [2, 6, 7, 8, 4, 0, 1, 5, 3] (center fixed) becomes SIGMA_C. What's currently SIGMA_C = [4, 8, 3, 2, 0, 7, 5, 6, 1] (full derangement) becomes SIGMA_A. The actual arrays don't move. The SIGMAS order stays [A, B, C, D]. The spec's "σ_A first" is now true. Zero downstream impact.
The old A/B/C/D correspondence to the four canonical squares via (p, q) was already removed from the constants block — those labels are just labels now. The only thing that matters is which permutation runs first.Pivoted toward simpler name-swapping solution insteadPivoted toward simpler name-swapping solution insteadNow the TS mirror:Verified array consistency across implementation filesVerified array consistency across implementation filesNow verify the SIGMAS array in the Rust file auto-derives from the constants:Verified consistency between array references and swapped valuesVerified consistency between array references and swapped valuesSIGMAS references the named constants so it automatically picks up the swap. Let me do a final consistency check:Prepared to transfer modified files to designated locationPrepared to transfer modified files to designated locationClean. Copy updated files to outputs:Validated the label swap approach's simplicityValidated the label swap approach's simplicityDone. Label swap only — the permutation arrays themselves didn't move. The SIGMAS order stays [A, B, C, D]. SIGMAS[round % 4] gives the derangement on round 0. The spec's "σ_A first for maximum disruption" is now true. Nothing downstream changes because nothing was referencing these by name yet — T-04 creates them for the first time.