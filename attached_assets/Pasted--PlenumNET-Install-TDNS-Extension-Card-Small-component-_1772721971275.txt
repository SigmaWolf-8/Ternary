/**
 * PlenumNET — Install TDNS Extension Card
 * 
 * Small component for the Developers > Get Started section.
 * NOT a full page — embed it inside any existing page layout.
 * Matches app theme: #090807 bg, #D4A017 gold, Inter 13px, shadcn-style.
 *
 * REPLIT INSTRUCTIONS:
 * 1. Save this file to: client/src/components/InstallExtensionCard.tsx
 * 2. Import and place it on the Get Started page:
 *      import InstallExtensionCard from "@/components/InstallExtensionCard";
 *      <InstallExtensionCard />
 * 3. Add nav link: Developers > Get Started > Install TDNS Extension
 *    pointing to the section/page that contains this component.
 * 4. Save install.ps1 to: services/tdns-v2/install.ps1
 * 5. In server/routes.ts, add:
 *      import path from "path";
 *      import fs from "fs";
 *      app.get("/install.ps1", (_req, res) => {
 *        const file = path.resolve("services/tdns-v2/install.ps1");
 *        if (fs.existsSync(file)) { res.type("text/plain").sendFile(file); }
 *        else { res.status(404).send("Not found"); }
 *      });
 */

import { useState } from "react";

export default function InstallExtensionCard() {
  const [copied, setCopied] = useState(false);

  const isWindows = typeof navigator !== "undefined" && navigator.userAgent.includes("Windows");
  const cmd = isWindows
    ? `irm ${window.location.origin}/install.ps1 | iex`
    : `curl -sL ${window.location.origin}/install.ps1 | bash`;

  const handleCopy = () => {
    navigator.clipboard.writeText(cmd);
    setCopied(true);
    setTimeout(() => setCopied(false), 3000);
  };

  return (
    <div className="rounded-lg border border-[#2A2520] bg-[#1A1816] p-5 max-w-md">
      <h3 className="text-sm font-semibold text-[#D4A017] tracking-wide uppercase mb-1">
        Install TDNS Extension
      </h3>
      <p className="text-xs text-[#8A8578] mb-4">
        Resolves .plm addresses in Chrome, Edge, Brave, Firefox, Opera &amp; Vivaldi.
      </p>

      <button
        onClick={handleCopy}
        className={`w-full py-2.5 px-4 rounded-md text-sm font-semibold transition-colors ${
          copied
            ? "bg-[#059669] text-white"
            : "bg-[#D4A017] text-[#090807] hover:bg-[#E8B42E]"
        }`}
      >
        {copied ? "✓ Copied — paste in PowerShell" : "Install Extension"}
      </button>

      {copied && (
        <div className="mt-3 rounded-md bg-[#0F0E0D] border border-[#2A2520] px-3 py-2">
          <code className="text-xs text-[#D4A017] font-mono break-all">{cmd}</code>
        </div>
      )}

      <p className="text-[10px] text-[#5A5548] mt-3">
        Auto-detects all installed browsers. Requires {isWindows ? "PowerShell" : "Terminal"}.
      </p>
    </div>
  );
}