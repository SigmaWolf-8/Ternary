[package]
name = "plenum-std"
version = "0.1.0"
edition = "2021"
authors = ["Capomastro Holdings Ltd <kernel@plenumnet.io>"]
description = "PlenumNET std shim — maps Rust std API surface to kernel primitives"
license = "LicenseRef-Proprietary"

[dependencies]
hashbrown = { version = "0.14", default-features = false, features = ["ahash", "allocator-api2"] }

[features]
default = []
